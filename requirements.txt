# =============================================================================
# IndexTTS2 API Server — Kaggle / Lightning Compatible requirements
# =============================================================================

# ---- API server ----
fastapi==0.115.4
starlette==0.41.2
uvicorn==0.32.0
python-multipart==0.0.12          # FastAPI file uploads (voice upload route)
httpx==0.27.2                     # inter-node request forwarding (load balancer)
anyio==4.6.2.post1
pydantic==2.9.2
pydantic-core==2.23.4
loguru==0.7.2

# ---- WebUI (mounted on Rank 0) ----
gradio==4.44.1
gradio-client==1.3.0

# ---- Torch stack ----
# Note: Using >= prevents breaking the environment's pre-installed CUDA-enabled torch builds.
torch>=2.4.0
torchaudio>=2.4.0
torchvision>=0.19.0
deepspeed==0.15.3
xformers>=0.0.27.post2            # DeepSpeed attention backend when available
ninja==1.11.1.1                   # JIT compile of DeepSpeed CUDA/CPP kernels

# ---- Model loaders / HF stack ----
transformers>=4.44.0
tokenizers>=0.19.0
accelerate>=0.34.0
safetensors>=0.4.5
huggingface-hub>=0.26.0
modelscope>=1.18.0                # checkpoint download fallback
omegaconf==2.3.0                  # config.yaml parsing
einops>=0.8.0
sentencepiece==0.2.0              # bpe.model tokenizer

# ---- Numerics / audio DSP ----
numpy>=1.26.0
scipy>=1.13.0
librosa>=0.10.2.post1             # silence trimming (librosa.effects.trim)
soundfile>=0.12.1                 # wav/flac I/O, prompt + output audio
soxr>=0.4.0                       # librosa resampling backend
audioread>=3.0.1                  # mp3/m4a reference audio decoding
pyloudnorm>=0.1.1
pydub>=0.25.1
lameenc>=1.7.0
ffmpeg-python>=0.2.0
av==11.0.0                        # Pinned strictly by audiocraft==1.3.0
matplotlib>=3.8.0                 # WebUI spectrogram/diagnostics plots

# ---- Text normalisation / G2P (zh + en) ----
wetextprocessing>=0.1.10          # >> TextNormalizer (zh/en fst)
pynini>=2.1.6.post1               # wetextprocessing backend
g2p-en>=2.1.0                     # english grapheme-to-phoneme
nltk>=3.9.1                       # g2p-en backend (download 'averaged_perceptron_tagger' once)
cn2an>=0.5.22                     # chinese number normalization
proces>=0.1.7                     # cn2an dependency
jieba>=0.42.1                     # chinese segmentation
num2words>=0.5.13

# ---- BGM / SFX: MusicGen (audiocraft) ----
audiocraft==1.3.0
sphn>=0.1.0                       # audiocraft audio I/O on GPU
julius>=0.2.7
argbind>=0.3.9
flatten-dict>=0.4.2
flashy>=0.0.2
demucs>=4.0.1                     # audiocraft source-separation dep chain

# ---- BGM / SFX: AudioX (HKUSTAudio) ----
# NOTE: The URL https://github.com/HKUSTAudio/AudioX.git prompts for credentials,
# meaning the repo is either private, renamed, or does not exist.
# Commenting out for now until the correct source/URL is confirmed.
# audiox @ git+https://github.com/HKUSTAudio/AudioX.git@main
# descript-audio-codec>=1.0.0       # DAC token (de)coding
# descript-audiotools>=0.7.4
# opencv-python-headless>=4.8.0     # video prompt reading (read_video)
# torchtyping>=0.1.5

# ---- Common transitives ----
requests>=2.32.0
tqdm>=4.66.0
filelock>=3.16.0
pyyaml>=6.0.1
pillow>=10.4.0
packaging>=24.1
regex>=2024.5.15
psutil>=6.0.0
click>=8.1.7