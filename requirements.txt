# =============================================================================
# IndexTTS2 API Server — production requirements
# Pinned from the live Lightning MMT environment (Python 3.12.11, Linux x86_64).
#
# Install:
#     pip install -r requirements.txt
#     pip install -e .                  # the local indextts package itself
#
# OS-level requirements pip cannot install:
#     - ffmpeg binary on PATH          (apt install ffmpeg / conda install ffmpeg)
#     - cloudflared binary             (only for --share public tunnel)
#     - CUDA driver capable of torch 2.13 (T4 works fine; nvidia-* wheels are
#       pulled automatically as torch dependencies)
#
# NVRTC/CUDA wiggle-rooms of your original env (cupy, nvidia-cuda-*, vllm, ray,
# jupyter, lightning-sdk, openai/anthropic clients) were left out on purpose —
# the server doesn't import them.
# =============================================================================

# ---- API server ----
fastapi==0.138.2
starlette==1.3.1
uvicorn==0.49.0
python-multipart==0.0.32          # FastAPI file uploads (voice upload route)
httpx==0.28.1                    # inter-node request forwarding (load balancer)
anyio==4.14.1
pydantic==2.13.4
pydantic-core==2.46.4
loguru==0.7.3

# ---- WebUI (mounted on Rank 0) ----
gradio==6.19.0
gradio-client==2.5.0

# ---- Torch stack ----
torch==2.13.0
torchaudio==2.11.0
torchvision==0.28.0
deepspeed==0.19.2
xformers==0.0.35                  # DeepSpeed attention backend when available
ninja==1.13.0                     # JIT compile of DeepSpeed CUDA/CPP kernels

# ---- Model loaders / HF stack ----
transformers==4.57.6
tokenizers==0.22.2
accelerate==1.14.0
safetensors==0.8.0
huggingface-hub==0.36.2
modelscope==1.38.0                # checkpoint download fallback
omegaconf==2.3.1                  # config.yaml parsing
einops==0.8.2
sentencepiece==0.2.1              # bpe.model tokenizer

# ---- Numerics / audio DSP ----
numpy==2.2.6
scipy==1.17.1
librosa==0.11.0                   # silence trimming (librosa.effects.trim)
soundfile==0.14.0                 # wav/flac I/O, prompt + output audio
soxr==1.1.0                       # librosa resampling backend
audioread==3.1.0                  # mp3/m4a reference audio decoding
pyloudnorm==0.2.0
pydub==0.25.1
lameenc==1.8.4
ffmpeg-python==0.2.0
av==18.0.0
matplotlib==3.11.0                # WebUI spectrogram/diagnostics plots

# ---- Text normalisation / G2P (zh + en) ----
wetextprocessing==1.2.0           # >> TextNormalizer (zh/en fst)
pynini==2.1.7                     # wetextprocessing backend
g2p-en==2.1.0                     # english grapheme-to-phoneme
nltk==3.9.4                       # g2p-en backend (download 'averaged_perceptron_tagger' once)
cn2an==0.5.24                     # chinese number normalization
proces==0.1.7                     # cn2an dependency
jieba==0.42.1                     # chinese segmentation
num2words==0.5.14

# ---- BGM / SFX: MusicGen (audiocraft) ----
audiocraft==1.3.0
sphn==0.2.1                      # audiocraft audio I/O on GPU
julius==0.2.8
argbind==0.3.9
flatten-dict==0.5.0
flashy==0.0.2
demucs==4.1.0                    # audiocraft source-separation dep chain

# ---- BGM / SFX: AudioX (HKUSTAudio) ----
audiox==0.2.0
descript-audio-codec==1.0.0       # DAC token (de)coding
descript-audiotools==0.7.2
opencv-python-headless==5.0.0.93  # video prompt reading (read_video)
torchtyping==0.1.5

# ---- Common transitives pinned for reproducibility ----
requests==2.34.2
tqdm==4.68.3
filelock==3.29.4
pyyaml==6.0.3
pillow==12.3.0
packaging==26.2
regex==2026.6.28
psutil==7.2.2
click==8.4.2

# =============================================================================
# Optional extras (installed in your env but NOT required by the server):
#   openai-whisper==20250625        # if you re-enable audio transcription helpers
#   spacy==3.8.14                   # NLP helpers for custom pipelines
#   prometheus-fastapi-instrumentator==8.0.2   # /metrics endpoint
#   webrtcvad==2.0.10               # VAD-based realtime streaming variants
# =============================================================================
